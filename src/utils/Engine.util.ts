import * as traverse from 'traverse';

import type { Schema, Types, Type } from '../types/Schema.type';
import type { TeyitOptions } from '../types/TeyitOptions.type';

export const validate = async (schema: Schema, fields: ) => {
  const traverse = 
};
